/*This function validates a card name and returns a list of valid card ids. Returns false if the name has no card equivalent*/

const validateCardName = (obj, options={type:"list"}) => {
}

export default validateCardName;